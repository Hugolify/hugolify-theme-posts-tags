module:
  mounts:
    - source: archetypes
      target: archetypes
    - source: assets
      target: assets
    - source: content
      target: content
    - source: i18n
      target: i18n
    - source: layouts
      target: layouts

# Default
permalinks:
  tags: /news/tags/:slug/
taxonomies:
  tag: tags

params:
  admin:
    collections:
      posts_tags:
        enable: true